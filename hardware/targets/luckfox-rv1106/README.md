# GarStreamTx on Luckfox RV1106

This is the application-owned RV1106 assignment profile formerly stored in
the reusable Target Pack. Select it explicitly with the workspace
`hardware.path` setting when running GarStreamTx on this board.

The default `GarStreamTx/hardware/` profile remains the Raspberry Pi 5 setup.
